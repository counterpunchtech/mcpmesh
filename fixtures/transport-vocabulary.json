{
  "note": "Canonical transport-vocabulary blocklist (mcpmesh §1.5/§17 / host-design §9). ONE copy — loaded by the host/loc suites AND mcpmesh's own surface-leak test (mcpmesh/cli/tests/daemon_autostart.rs); the kb suite (plugins/kb/daemon/kb/tests/surface_leak_km2.rs) still carries an inline copy that should converge here; do NOT add another inline copy. 'serial'/'relay' match as whole IDENTIFIER tokens (_ is a word char) so domain compounds like index_serial never false-trip (R4). Carve-outs are spec-canonical identifiers that legitimately embed a banned substring.",
  "substring_banned": [
    "endpoint",
    "endpointid",
    "ticket",
    "alpn",
    ".sock",
    "iroh",
    "quic",
    "nodeid",
    "pubkey"
  ],
  "token_banned": ["serial", "relay"],
  "carve_outs": ["ask_model_endpoint"]
}
